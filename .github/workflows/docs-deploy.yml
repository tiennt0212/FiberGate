# Builds docs/ (VitePress) and deploys to GitHub Pages on every push to
# canary that touches docs/**. Uses actions/deploy-pages + configure-pages +
# upload-pages-artifact — OIDC-based, built-in GITHUB_TOKEN only, same
# "no third-party secret" posture as docker-publish.yml / npm-publish.yml.
#
# One-time manual prerequisite (cannot be automated): repo Settings -> Pages
# -> Source: "GitHub Actions".
name: Deploy docs site

on:
  push:
    branches: [canary]
    paths:
      - "docs/**"
  workflow_dispatch: {}

concurrency:
  group: "pages"
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build docs site
        run: pnpm --filter docs build

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
