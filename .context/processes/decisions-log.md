---
type: decisions_log
version: 1.0
last_updated: 2026-07-07T23:59+07:00
tags: [decisions, architecture, business-logic, open-questions]
---

# Decisions Log — FiberGate

> Ghi lại các quyết định đã được **human xác nhận**.
> Claude Code cập nhật file này **sau khi** human confirm trong cuộc hội thoại.
> KHÔNG tự thêm khi chưa có xác nhận rõ ràng từ human.
>
> Format: `[YYYY-MM-DD] **Topic**: decision — Lý do: why`

---

## Architecture & Tech Stack

[2026-07-01] **Product framing pivot**: Chuyển FiberGate từ managed custodial LSP (SaaS đa khách hàng) sang self-hosted open-source infrastructure framework — đóng gói docker-compose (Fiber node + PostgreSQL + FiberGate core: Next.js dashboard + API), mỗi merchant tự deploy `docker compose up -d` và giữ node/dữ liệu của họ — Lý do: Hackathon "Gone in 60ms" quy định rõ "infrastructure only, not products built on top"; mô hình SaaS custodial đa khách hàng có rủi ro bị xếp sai category và mất điểm.

[2026-07-01] **Auth model**: Bỏ multi-tenant API key system (`profiles`/`api_keys` table, bcrypt `secret_hash` theo từng client, BR-SEC-004 user_id scoping). Thay bằng 1 lớp internal auth đơn giản: single shared secret `FIBERGATE_INTERNAL_SECRET` (giống pattern `CRON_SECRET` đã có), so sánh constant-time, dùng để app bán hàng của merchant gọi vào FiberGate core khi 2 service không chạy chung host/network — Lý do: Trong self-hosted, mỗi deployment chỉ phục vụ 1 merchant nên phân biệt nhiều client là dư thừa; vẫn cần auth nội bộ tối thiểu để core không bị gọi trái phép khi tách container ra mạng riêng.

[2026-07-01] **Database**: Bỏ Supabase (kể cả bản self-hosted), chuyển sang PostgreSQL thuần (container riêng) + Drizzle ORM — Lý do: Bộ self-hosted Supabase chính thức đóng gói ~10 container (gotrue, postgrest, realtime, storage, kong, studio...), quá nặng so với mục tiêu "3 container gọn nhẹ" của docker-compose bundle; single-tenant nên không cần Supabase Auth/RLS.

[2026-07-01] **Dashboard auth**: Dùng single-admin password gate (đặt qua env var lúc deploy, hash bcrypt, session httpOnly cookie) thay vì Supabase Auth/multi-user login — Lý do: Mỗi deployment chỉ có 1 merchant vận hành, không cần hệ thống user/role.

[2026-07-01] **Fiber client SDK**: `lib/fiber/client.ts` build trên nền `@ckb-ccc/fiber` (official SDK), không tự viết JSON-RPC thô, không dùng `@fiber-pay/sdk` cho core flow — Lý do: `@ckb-ccc/fiber` là SDK chính thức được hackathon khuyến nghị ("best starting point for app integrations"), `@fiber-pay/sdk`/`@fiber-pay/react` là community/experimental, rủi ro hơn cho core flow (Phase 1/2). `@fiber-pay/sdk` được giữ lại làm reference tham khảo cho Phase 3 (L402) vì có demo `fiber-l402` dùng đúng thư viện này.

[2026-07-01] **Phase 2 real-time listener — verified**: Xác nhận FNN có RPC module `pubsub` (method `subscribe_store_changes`/`unsubscribe_store_changes`, notification `store_changes`), tồn tại từ bản stable v0.8.1 (không phải chỉ nhánh develop), có document chính thức tại `fiber.world/docs/api-reference#websocket-subscriptions`. Node bắn `StoreChange::PutCkbInvoiceStatus { payment_hash, invoice_status }` đúng lúc invoice đổi trạng thái — Lý do quyết định dùng cho Phase 2: đây là cách duy nhất tránh polling định kỳ, khớp với tốc độ thanh toán tức thời của Fiber Network. Ràng buộc đã biết: (a) doc chính thức ghi rõ mục đích thiết kế là cho Cross-Chain Hub, không phải general client — coi là "off-label usage", vẫn giữ poll fallback tần suất thấp (30-60s); (b) `@ckb-ccc/fiber` không hỗ trợ subscription (đã verify qua source npm thật) — cần viết 1 WebSocket JSON-RPC client riêng cho phần này; (c) nếu bật Biscuit auth cần thêm quyền `read("cch")`, nhưng vì `fiber-node` chỉ bind nội bộ trong docker network (không public IP) nên dự kiến không bật Biscuit auth, ràng buộc này không phát sinh trong setup mặc định.

[2026-07-02] **SDK package naming — sửa mâu thuẫn**: Xác nhận tên package chính thức là `@fibergate/sdk` (khớp `.context/INDEX.md`, `system-design.md`, và issue #2 checklist — 3/4 nguồn đồng thuận). `CLAUDE.md`'s dòng monorepo layout trước đó ghi nhầm `@fiber-gateway/sdk` — đã sửa lại trong cùng session — Lý do: CLAUDE.md tự nhận là single source of truth nên khi phát hiện sai lệch, sửa CLAUDE.md để khớp đa số nguồn còn lại thay vì giữ nguyên rồi sửa các file khác.

[2026-07-02] **fiber-node container — build strategy** (issue #3, docker-compose bundle): Dùng **official Docker image** `nervos/fiber` (Docker Hub) / `ghcr.io/nervosnetwork/fiber` (GHCR mirror) cho service `fiber-node`, thay vì tự build FNN từ source hoặc tự đóng gói prebuilt binary — Lý do: `nervosnetwork/fiber` repo (`docker/README.md`, xác nhận trực tiếp qua `gh api`) publish official image kèm sẵn `fnn-cli`, tự động copy bundled testnet config vào `/fiber/config.yml` lúc first-run — đơn giản và ít rủi ro hơn hẳn so với tự build/maintain Dockerfile cho FNN. Ràng buộc phát sinh: image repo **hiện chưa có tag semver ổn định nào** (chỉ có `0.9.0-rc1`..`rc6` prerelease và `sha-<hash>`) dù GitHub release `v0.8.1` đã tồn tại — pin tạm `nervos/fiber:0.9.0-rc6` (tag mới nhất, có image thật), cần revisit khi có stable tag. Gotcha quan trọng: RPC listener trong bundled testnet config mặc định bind `127.0.0.1:8227` (loopback-only) — phải pre-seed `docker/fiber-node/config.yml` với `rpc.listening_addr: 0.0.0.0:8227` thì `fibergate-core` mới gọi được qua internal Docker network; nếu không sẽ silently unreachable dù `depends_on`/network đều đúng.

[2026-07-02] **Base images cho docker-compose** (issue #3): `node:20-alpine` cho `docker/fibdergate-core/Dockerfile` (fibergate-core), `postgres:16-alpine` cho service `postgres` — Lý do: Node 20 là LTS hiện hành, Postgres 16 là bản stable gần nhất; không có ràng buộc CKB toolchain nào yêu cầu base image khác, chọn Alpine để giữ image nhỏ gọn khớp mục tiêu "3 container gọn nhẹ" đã chốt ở decision "Database" phía trên.

[2026-07-02] **`docker/fiber-node/config.yml` — sửa provenance sai** (phát hiện lúc review lại issue #3 sau khi merge): Bản config commit ban đầu lấy từ `gh api .../contents/config/testnet/config.yml` **không kèm `?ref=`**, tức lấy default branch HEAD thay vì đúng tag `v0.9.0-rc6` (khớp Docker image tag `nervos/fiber:0.9.0-rc6` đang pin) — 2 bản lệch nhau thật (HEAD có thêm SOCKS5/Tor proxy options chưa có ở `v0.9.0-rc6`). Đã fetch lại đúng `?ref=v0.9.0-rc6`, chỉ giữ đúng 1 thay đổi có chủ đích (`rpc.listening_addr: 127.0.0.1` → `0.0.0.0`) — Lý do: config fetch không pin đúng ref là rủi ro schema-mismatch âm thầm, nguyên tắc chung: mọi tài nguyên fetch từ 1 repo bên ngoài phải pin đúng ref khớp với version đang thực sự chạy.

[2026-07-02] **`FIBER_SECRET_KEY_PASSWORD` — thêm vào `.env.example`** (phát hiện lúc review lại issue #3 sau khi merge): Trước đó biến này cố tình bị loại khỏi `.env.example` (tách bạch khái niệm "7 biến app-level của FiberGate" khỏi "secret riêng của fiber-node"). Đổi quyết định: vẫn thêm 1 dòng template cho nó, nhưng để trong section riêng có comment giải thích rõ **không** thuộc 7 biến app-level — Lý do: tách bạch khái niệm không bắt buộc phải đánh đổi bằng việc loại hẳn khỏi file mẫu; merchant làm theo đúng flow chính (`cp .env.example .env`) trước đó sẽ không thấy biến này ở đâu cả, dễ bỏ sót vì thông tin duy nhất nằm ở 1 dòng prose riêng trong `docker-compose.yml`/`README.md`. Ưu tiên UX (giảm rủi ro bỏ sót credential thật) hơn tính thuần khiết của phân loại tài liệu, vì không phát sinh rủi ro security nào khi thêm (biến vẫn đọc qua đúng 1 cơ chế `.env` → `docker-compose.yml`'s `${VAR}` substitution như 7 biến kia). Đồng bộ cập nhật: `.env.example`, `README.md` ("Generating secrets" giờ là 5 biến), `system-design.md`.

[2026-07-02] **`DATABASE_URL` derive tự động từ `POSTGRES_*` ở root** (bước 1/2, sau đó bị thay bởi quyết định "1 code path duy nhất" bên dưới): Bỏ field `DATABASE_URL=` nhập tay ở root `.env.example` — Lý do ban đầu: đây là bug duplicate **trong cùng 1 file** — `DATABASE_URL` và 3 biến `POSTGRES_*` cùng encode 1 thông tin (credentials) ở chung root `.env.example`, sửa 1 chỗ quên chỗ kia sẽ ra lỗi auth khó đoán.

[2026-07-02] **Đổi tên `FIBER_NODE_SECRET` → `FIBER_NODE_RPC_AUTH_TOKEN`** (human chọn trực tiếp trong hội thoại, giữa 4 lựa chọn được đề xuất): Biến Biscuit token cấp app (optional, dùng khi gọi RPC tới fiber-node) dễ nhầm với `FIBER_SECRET_KEY_PASSWORD` — biến cấp node do chính image `nervos/fiber` quy định tên, không đổi được. Đổi sang tên rõ công dụng hơn (RPC auth token) mà không gắn cứng vào cơ chế Biscuit cụ thể — Lý do: giảm rủi ro nhầm lẫn giữa 2 khái niệm khác hẳn nhau (auth cho app gọi RPC vs password mở khóa CKB signing key) nằm rải rác ở nhiều file khác nhau (`.env.example` x2, `docker-compose.yml`, `system-design.md`).

[2026-07-02] **Thêm `fiber-node-preflight` service** (human chọn "cả 2" giữa "chỉ cải thiện docs" và "thêm preflight check", tức làm cả 2): Thêm 1 one-shot service dùng `alpine:3.20`, kiểm tra `docker/fiber-node/ckb/key` tồn tại trước khi `fiber-node` được phép start (`depends_on: condition: service_completed_successfully`), in thông báo tiếng người rõ ràng nếu thiếu thay vì để `fiber-node` tự crash-loop với Rust panic khó đọc — Lý do: merchant lần đầu deploy quên bước tạo CKB key sẽ thấy lỗi mơ hồ nếu không có preflight; đã verify live cả 2 nhánh (thiếu key → exit 1 kèm message rõ; có key → pass). README cũng được bổ sung mục Prerequisites + Troubleshooting tương ứng.

[2026-07-02] **`apps/web/.env.local` đổi sang `POSTGRES_USER/PASSWORD/DB/HOST/PORT`** (bước 2/2, human chọn trực tiếp giữa "giữ nguyên DATABASE_URL" vs "đổi sang POSTGRES_*"): Mục tiêu chính là đồng bộ pattern giữa 2 file env, không phải vì `DATABASE_URL` "không dùng nữa" — biến này vẫn được dùng thật, chỉ đổi ai ghép chuỗi. Trước đó root dùng bộ `POSTGRES_*` (derived qua compose) còn `apps/web` dùng `DATABASE_URL` (nhập tay) — 2 pattern khác nhau cho cùng 1 khái niệm. `POSTGRES_HOST`/`POSTGRES_PORT` cần thêm riêng ở `apps/web` (mặc định `localhost:5432`) vì local dev có thể trỏ tới bất kỳ Postgres nào, khác với topology cố định trong docker.

[2026-07-02] **Chốt lại: bỏ hẳn `DATABASE_URL` khỏi mọi nơi (kể cả `docker-compose.yml`), luôn để code tự ghép** (human chốt trực tiếp, thay thế cách làm ở 2 quyết định ngay trên): `docker-compose.yml`'s `fibergate-core.environment` không còn ghép `DATABASE_URL` qua `${VAR}` interpolation nữa — thay bằng truyền thẳng 5 biến `POSTGRES_USER/PASSWORD/DB/HOST/PORT` (với `HOST=postgres`, `PORT=5432` là giá trị cố định khai trong `docker-compose.yml`, không phải field `.env.example`). `lib/db/` (chưa viết) giờ có **đúng 1 code path** để build connection string, dùng chung cho cả chạy trong Docker lẫn `pnpm dev` ngoài Docker — không còn phân biệt "root derive qua compose, apps/web derive qua code" như 2 quyết định trước. Lý do: đơn giản hoá về đúng 1 cơ chế, giảm bề mặt cần giải thích trong `system-design.md` và giảm rủi ro 2 cơ chế song song lệch nhau sau này.

[2026-07-02] **Thêm `dotenv-cli` làm dev dependency của `apps/web`, gộp root `.env` + `apps/web/.env.local` cho local dev** (human xác nhận thêm dependency mới trực tiếp trong hội thoại, sau khi tôi hỏi và có sự đồng ý rõ ràng — đúng quy trình CLAUDE.md yêu cầu cho "cài thêm dependency mới"): `apps/web/package.json`'s `dev` script đổi từ `next dev` thành `dotenv -e .env.local -e ../../.env -- next dev`. Đã verify live: `dotenv-cli` "file liệt kê trước thắng" (theo docs chính thức, không phải "sau thắng" — dễ nhầm), nên `.env.local` phải đứng trước `../../.env` trong lệnh để override đúng; đã test `next dev` khởi động thành công với setup này. Hệ quả: `apps/web/.env.example` co lại còn đúng 3 biến thực sự khác nhau giữa Docker/local dev (`POSTGRES_HOST`, `POSTGRES_PORT`, `FIBER_NODE_URL`) thay vì duplicate toàn bộ 10 biến như trước — root `.env` giờ là nguồn thật duy nhất cho các biến chung (`POSTGRES_USER/PASSWORD/DB`, `ADMIN_PASSWORD_HASH`, `FIBERGATE_INTERNAL_SECRET`, `FIBER_NODE_RPC_AUTH_TOKEN`, `WEBHOOK_SIGNING_KEY`, `CRON_SECRET`). `docker-compose.yml` không đổi gì — Docker Compose đã tự đọc root `.env` theo convention có sẵn, không cần `dotenv-cli`. Lý do: giảm triệt để duplicate secret giữa 2 file, gốc của toàn bộ chuỗi thảo luận về `DATABASE_URL`/đồng bộ pattern ở các quyết định phía trên.

[2026-07-02] **`.env.example` pre-fill placeholder `CHANGE_ME_*` + service `secrets-preflight` — LÀM RỒI SAU ĐÓ REVERT, xem quyết định ngay dưới** (giữ lại làm lịch sử, không xoá — thấy cách tiếp cận này dài dòng nên đã đổi hướng trong cùng phiên): Từng thêm 4 biến `CHANGE_ME_<TÊN_BIẾN>` + 1 service `alpine` one-shot chặn `postgres`/`fibergate-core` nếu còn placeholder. Bug đáng nhớ phát sinh lúc implement (vẫn còn giá trị tham khảo dù đã revert code): Docker Compose tự interpolate `$VAR`/`${VAR}` trên TOÀN BỘ string trong YAML, kể cả bên trong khối `command: |` nhiều dòng — biến shell nội bộ của script (`$val`, `$var`) bị Compose hiểu nhầm thành biến compose chưa set, tự thay bằng rỗng, làm check luôn pass sai (silent bug). Nếu sau này cần viết shell script bên trong `command:` của bất kỳ service nào khác, nhớ escape mọi `$` shell-runtime thành `$$`.

[2026-07-02] **Revert `secrets-preflight`, quay lại để trống 4 biến required, hướng dẫn generate chuyển sang README.md** (human chốt trực tiếp, thay thế quyết định ngay trên): Thêm hẳn 1 service chỉ để validate giá trị `.env` bị đánh giá là quá dài dòng cho lợi ích mang lại. Quay lại pattern đơn giản: để trống (không placeholder), `docker compose up -d` tự fail rõ ràng ở `postgres`/`fibergate-core` nếu thiếu (lỗi credential rỗng) — đủ tín hiệu cho merchant, không cần thêm container riêng chỉ để check. Hướng dẫn generate từng secret (`openssl rand -hex 32`, `docker run --rm httpd:alpine htpasswd -nbBC 10 admin '...'`) chuyển từ comment trong `.env.example` sang mục "Generating secrets" trong `README.md` — file `.env.example` gọn lại, hướng dẫn chi tiết có chỗ riêng dễ đọc hơn. Verify live: `docker compose up -d postgres` với `POSTGRES_PASSWORD` rỗng — chính image `postgres` chính thức tự chặn, in lỗi rõ ràng ("Database is uninitialized and superuser password is not specified...") và restart-loop, đủ tín hiệu, không cần custom preflight.

[2026-07-02] **`fiber-node` publish RPC loopback-only (`127.0.0.1:8227:8227`)** (human chốt trực tiếp, sau khi tôi phân tích rủi ro của việc publish public và đề xuất bind loopback thay vì để trống hoặc bind `0.0.0.0`): Ban đầu `fiber-node` không có `ports:` nào (đúng yêu cầu issue #3 "not bind a public IP/port"), nhưng hệ quả là `pnpm dev` (chạy `apps/web` ngoài Docker) không gọi được `fiber-node` dù nó đang chạy — `FIBER_NODE_URL=http://localhost:8227` gợi ý trước đó trong `apps/web/.env.example` thực ra không hoạt động vì chưa có port nào publish. Thêm `"127.0.0.1:8227:8227"` giải quyết đúng vấn đề mà không vi phạm yêu cầu gốc: bind loopback-only không bao giờ lộ ra ngoài máy đang chạy, kể cả khi máy đó là VPS có IP public — khác hẳn `"8227:8227"` thường (mặc định `0.0.0.0`, sẽ lộ RPC chưa bật Biscuit auth ra internet). Đã verify live: `docker compose ps` hiện đúng `127.0.0.1:8227->8227/tcp`, `ss -tlnp` xác nhận socket chỉ LISTEN trên `127.0.0.1` không phải `0.0.0.0`. `apps/web/.env.example`'s `FIBER_NODE_URL=http://localhost:8227` giờ pre-fill được vì đã thật sự hoạt động (trước đó để trống vì chưa chắc).

[2026-07-02] **Sự cố lúc chạy thật `docker compose up -d` lần đầu (issue #3 đã merge nhưng chưa ai chạy live) — 3 lỗi liên hoàn, xử lý tuần tự:**

Bối cảnh: human chạy `docker compose up -d` theo đúng README, gặp lỗi ngay ở bước đầu tiên. Xử lý từng lỗi phát sinh, mỗi lỗi che khuất lỗi tiếp theo cho tới khi fix xong:

1. **`fiber-node-preflight` exit 1 — "missing CKB testnet key"**: Đúng hành vi thiết kế của preflight (xem quyết định "Thêm `fiber-node-preflight` service" phía trên) — `docker/fiber-node/ckb/key` chưa tồn tại. Không phải bug, chỉ là bước setup chưa làm.

2. **`ADMIN_PASSWORD_HASH` bị Docker Compose corrupt âm thầm qua `.env` interpolation**: Cùng lúc với lỗi 1, thấy warning `"The \"NdL\" variable is not set. Defaulting to a blank string."`. Nguyên nhân: bcrypt hash `$2y$10$NdL.ph3...` trong `.env` chứa nhiều dấu `$` (`$2y`, `$10`, `$NdL`), và Docker Compose tự interpolate `$VAR`/`${VAR}` trên **giá trị bên trong `.env`**, không chỉ trong `docker-compose.yml` — mỗi cụm `$xxx` bị hiểu nhầm thành biến compose chưa set, tự thay bằng rỗng. Verify bằng `docker compose config | grep ADMIN_PASSWORD_HASH`: hash bị mất hẳn đoạn `NdL.` giữa chừng, tức đăng nhập dashboard chắc chắn sẽ fail dù không có bất kỳ error message rõ ràng nào lúc `up -d`. Fix: escape mọi `$` trong giá trị hash thành `$$` (`ADMIN_PASSWORD_HASH=$$2y$$10$$NdL.ph3...`), verify lại bằng `docker compose config` thấy resolve đúng nguyên bản gốc. Đây là biến thể khác của gotcha đã ghi ở quyết định "`CHANGE_ME_*` — LÀM RỒI SAU ĐÓ REVERT" phía trên (lúc đó là `$` trong khối `command:` nhiều dòng của 1 service; lần này là `$` trong chính giá trị 1 biến `.env` thường, không liên quan `command:` gì cả) — kết luận chung: **bất kỳ giá trị nào đặt trong `.env` mà chứa ký tự `$` (bcrypt hash là ví dụ hay gặp nhất, cũng có thể là 1 số dạng base64/token) đều phải escape thành `$$`**, không chỉ riêng bên trong khối `command:` của service.

3. **`fiber-node` crash-loop: `Secret key file error: decryption failed: aead::Error`**: Sau khi thêm key vào `docker/fiber-node/ckb/key`, `fiber-node-preflight` pass nhưng `fiber-node` tự crash-loop với lỗi decrypt dù `FIBER_SECRET_KEY_PASSWORD` xác nhận đúng (human verify bằng `ckb-cli account export --lock-arg ... --extended-privkey-path key2` với đúng password đó, export thành công). Tức lỗi không phải password sai mà là **format file sai**: `ckb-cli account export --extended-privkey-path` xuất ra file **2 dòng** (dòng 1 = raw private key, dòng 2 = chain code của extended key), còn `fnn` chỉ chấp nhận **đúng 1 dòng raw private key hex, không `0x` prefix, không chain code**. File key đang có nguyên cả 2 dòng. Xác nhận bằng cách đo kích thước/số dòng file (không in nội dung key ra terminal): `wc -c` = 129 bytes = 2 dòng × 64 hex-char + 1 newline, khớp giả thuyết. Fix: `head -n 1 docker/fiber-node/ckb/key > tmp && mv tmp docker/fiber-node/ckb/key && chmod 600 ...` — chỉ giữ dòng đầu.

4. **`fiber-node` chạy được (`secret key migration done`) nhưng RPC server từ chối start — "Cannot listen on a public address without a biscuit public key set in the config"**: Nguồn gốc là `docker/fiber-node/config.yml`'s `rpc.listening_addr: 0.0.0.0:8227` — giá trị này được set chủ đích từ quyết định "build strategy" ở trên với lý do "để sibling container gọi được qua docker internal network", đã hoạt động đúng lúc review code nhưng **chưa từng chạy live tới bước này** nên chưa lộ vấn đề. `fnn` (bản đang pin `0.9.0-rc6`) enforce cứng lúc startup: từ chối bind bất kỳ địa chỉ nào nó tự phân loại "public" trừ khi có `rpc.biscuit_public_key`. Xác nhận qua 2 nguồn: (a) log lỗi runtime thật; (b) tra CKB AI MCP (`mcp__ckb-ai__search_resources` → `ReadMcpResourceTool` đọc `ckb://docs/fiber/node-setup`, mục "Safe RPC Exposure") — nguyên văn: *"Hard rule, enforced at startup: the node refuses to start if `rpc.listening_addr` is a public address and `rpc.biscuit_public_key` is not set... A 'public address' is anything not loopback / private / link-local (including `0.0.0.0`)."* — tức **`0.0.0.0` bị `fnn` tự xếp vào nhóm "public" dù thực tế chỉ chạy trên docker network nội bộ, không có route nào ra ngoài** — giả định ngầm ở quyết định "build strategy" phía trên ("bind nội bộ thì không cần lo public/Biscuit gì") sai trong thực tế, dù đúng tinh thần bảo mật.

   Cân nhắc 2 hướng xử lý, đã trình bày rõ trade-off cho human trước khi chọn:
   - **(a) Setup Biscuit auth đầy đủ**: đúng cơ chế `fnn` thiết kế sẵn cho non-loopback bind (gen ed25519 keypair, mint scoped token theo domain `read`/`write` cho từng RPC module). Kiểm tra thấy image `nervos/fiber` **không bundle** tool gen/mint Biscuit nào (`fnn --help`, `fnn-cli --help`, list binary trong image đều không có) — phải cài thêm tool ngoài (vd `biscuit-cli` qua `cargo install` trong 1 container tạm), tức **thêm dependency mới** (CLAUDE.md yêu cầu phải hỏi trước) cộng thêm chi phí vận hành (quản lý/rotate token) cho 1 kết nối **luôn luôn chỉ nội bộ Docker**, không có route ra ngoài.
   - **(b) Gán static private IP cho `fiber-node`** thay vì wildcard `0.0.0.0`: doc troubleshooting table của chính `fnn` cũng gợi ý hướng này ("...set it or bind to `127.0.0.1`" — dùng `127.0.0.1` không khả thi ở đây vì đó là loopback *bên trong* container `fiber-node`, không container nào khác (`fibergate-core`) gọi tới được qua đó; cần 1 IP thuộc dải private nhưng nằm trên network interface thật mà container khác reach tới được — tức chính IP của container đó trên bridge network). Không thêm dependency, effective security không đổi (vẫn 0 route ra ngoài docker network nội bộ, y hệt lúc còn `0.0.0.0`).

   Human chọn (b) — Lý do: giữ scope tối giản đúng tinh thần hackathon, tránh thêm dependency/vận hành không tương xứng lợi ích cho 1 kết nối không bao giờ public. Implement: `docker-compose.yml`'s network `fibergate-net` thêm `ipam.config.subnet: 172.28.0.0/24`; service `fiber-node` gán `networks.fibergate-net.ipv4_address: 172.28.0.10`; `docker/fiber-node/config.yml`'s `rpc.listening_addr` đổi từ `0.0.0.0:8227` → `172.28.0.10:8227`. Hệ quả kéo theo phải sửa thêm: healthcheck `["CMD", "fnn-cli", "info"]` (mặc định trỏ `127.0.0.1`, chạy *trong* chính container `fiber-node`) không còn reach được RPC vì RPC giờ chỉ bind đúng `172.28.0.10`, không phải wildcard nữa — đổi thành `["CMD", "fnn-cli", "-u", "http://172.28.0.10:8227", "info"]`. `FIBER_NODE_URL=http://fiber-node:8227` (biến `fibergate-core` dùng để gọi sang) **không cần đổi** — resolve qua Docker DNS theo service name, không phụ thuộc IP là static hay dynamic, y hệt cách `POSTGRES_HOST=postgres` đã hoạt động từ trước.

   Đã verify live: `docker compose up -d` → `docker compose ps` hiện `fiber-node` "healthy" (trước đó "unhealthy"/exit); `docker compose logs fiber-node` xác nhận tuần tự `secret key is using plain key format, start migrating to encrypted format` → `secret key migration done` → RPC start thành công, không còn log lỗi biscuit; cả 3 container (`postgres`, `fiber-node`, `fibergate-core`) đều "healthy"/"running", `fibergate-core` log `Ready in 96ms`.

   **Việc cần làm tiếp** (chưa làm trong phiên này): đồng bộ lại `system-design.md` (dòng ghi `rpc.listening_addr: 0.0.0.0:8227` ở mục "fiber-node container" và giả định "bind nội bộ thì Biscuit auth có thể tắt hẳn" ở mục websocket subscription — cả 2 đều dựa trên thiết kế cũ, nay sai) và bổ sung `README.md`'s Troubleshooting với 3 gotcha ở trên (preflight, `$` escaping, key file 1 dòng, static IP/biscuit).

[2026-07-03] **Postgres driver package cho Drizzle (issue #4)**: Dùng package `postgres` (lightweight, promise-native) làm driver, cùng `drizzle-orm` (dependency) và `drizzle-kit` (devDependency) — thêm mới cả 3 vào `apps/web/package.json` — Lý do: human chọn trực tiếp giữa `postgres` vs `pg`+`@types/pg` khi được hỏi (đúng quy trình CLAUDE.md cho "cài thêm dependency mới"); `postgres` là driver Drizzle's own docs mặc định dùng cho Postgres, ít boilerplate hơn `pg`.

[2026-07-03] **Migration chạy thủ công, không tự động lúc container boot (issue #4)**: Không sửa `docker/fibergate-core/Dockerfile` (entrypoint wrapper) hay thêm one-shot service vào `docker-compose.yml` (kiểu `fiber-node-preflight`) để tự chạy migration. Thay vào đó thêm 2 script `db:generate`/`db:migrate` (`apps/web/package.json`, dùng thẳng `drizzle-kit migrate` làm "script thủ công" — không cần thêm TS runner như `tsx`/`ts-node`) — merchant tự chạy `pnpm --filter web db:migrate` một lần sau `docker compose up -d` (đã bổ sung bước này vào `README.md`'s "Running the full stack") — Lý do: human chọn phương án này giữa 3 lựa chọn (entrypoint wrapper / one-shot compose service / manual script) khi được hỏi — đây là architecture choice có nhiều hướng khả thi mà CLAUDE.md yêu cầu hỏi human trước.

[2026-07-03] **`invoices.status` dùng Drizzle `pgEnum` thay vì `text` (issue #4)**: `.context/data-dictionary/database-schema.md` trước đó ghi `status` là `text`; đổi sang Postgres native enum `invoice_status` (`pending`/`paid`/`expired`/`failed`) qua Drizzle's `pgEnum`, đã đồng bộ cập nhật `database-schema.md` trong cùng iteration. `webhook_deliveries.status` (khác domain) không đổi, vẫn là `text` — Lý do: human chọn hướng này khi được hỏi, cho rằng `status` là tập giá trị cố định nên enum ở tầng DB hợp lý hơn text tự do; không đổi rule chuyển trạng thái 1 chiều đã chốt trước đó (BR-STS-001: `pending → paid | expired | failed`, không có chiều ngược, vẫn chỉ enforce ở application code, không có DB trigger/constraint).

[2026-07-03] **`postgres` publish port loopback-only (`127.0.0.1:5432:5432`) — phát hiện lúc human tự chạy thử `pnpm --filter web db:migrate` sau khi merge issue #4**: Service `postgres` trong `docker-compose.yml` trước đó không có `ports:` nào (chỉ nằm trong network nội bộ `fibergate-net`), nên `pnpm --filter web db:migrate`/`pnpm dev` chạy trên host (`POSTGRES_HOST=localhost` theo `apps/web/.env.local`) không kết nối được — verify bằng cách test socket `localhost:5432` từ host: connection refused. Kiểm tra thêm phát hiện chạy migration từ **trong** container `fibergate-core` cũng không được: `docker/fibergate-core/Dockerfile` build production theo `output: standalone`, không copy `apps/web/lib/db/migrations/` và không cài `drizzle-kit` (devDependency bị loại). Tức quyết định "manual migration script" (issue #4, xem quyết định phía trên) trước đó chưa có đường nào thật sự chạy được đối với Postgres trong docker-compose. Đã trình bày 3 hướng cho human trước khi chọn: (a) publish loopback-only, (b) publish thường `0.0.0.0` (đơn giản hơn nhưng lộ ra internet nếu host có public IP không firewall), (c) không sửa compose, dùng container tạm attach vào network để migrate. Human chọn (a) — Lý do: giống hệt pattern đã chốt cho `fiber-node` RPC (2026-07-02), loopback-only không bao giờ lộ ra ngoài máy đang chạy dù máy đó có IP public, đơn giản hơn (c) mà vẫn giữ nguyên tinh thần bảo mật. Đã verify live: recreate container `postgres` → `docker compose ps` hiện `127.0.0.1:5432->5432/tcp` → `pnpm --filter web db:migrate` chạy thành công (`migrations applied successfully!`) → `psql \dt`/`\dT+` xác nhận đúng 4 bảng + enum `invoice_status` (4 giá trị) đã tồn tại thật trong DB.

[2026-07-03] **`@ckb-ccc/fiber@0.0.0-canary-20260505020844` — verified API surface khi implement `lib/fiber/client.ts` (issue #5)**: Cài thật package (pin exact version, human đã approve dependency mới này trực tiếp) và đọc `src/` thật (không phải bản extract từ session trước) để xác nhận API trước khi code, phát hiện 2 điểm đáng ghi lại:
  1. **`FiberSDKConfig.timeout` là abort thật, không chỉ connect-timeout**: Đọc `@ckb-ccc/core`'s `src/jsonRpc/transports/http.ts` xác nhận `TransportHttp` dùng `AbortController` + `setTimeout(() => aborter.abort(), timeout)` bọc quanh `fetch` — tức truyền `timeout: 5000` vào `new FiberSDK({ endpoint, timeout })` (client.ts) thực sự abort request đang bay giữa chừng sau 5s, khớp đúng BR-POL-004, không cần tự viết `Promise.race`/`AbortController` thủ công.
  2. **`FIBER_NODE_RPC_AUTH_TOKEN` hiện KHÔNG có đường nào để truyền vào SDK**: Đọc toàn bộ `src/` của `@ckb-ccc/fiber` + `@ckb-ccc/core`'s `jsonRpc/` (`grep -rn "biscuit|auth|header|bearer|token"`) không thấy dòng nào — `FiberSDKConfig`/`FiberClientConfig` chỉ nhận `{ endpoint, timeout, maxConcurrent, fallbacks, transport }`, và `TransportHttp.request()` hard-code header duy nhất là `content-type: application/json`. Muốn gắn Biscuit token vào request thật sự cần tự viết 1 `Transport` tuỳ chỉnh (interface cho phép qua field `transport`) — việc đó nằm ngoài scope issue #5 (wrap SDK, không tự viết lại transport layer). `client.ts` vẫn đọc `process.env.FIBER_NODE_RPC_AUTH_TOKEN` trực tiếp (theo đúng yêu cầu issue #5) và export ra để chỗ nào cần sau này dễ thấy, nhưng **hiện tại giá trị này không có tác dụng thật** — không phải bug của issue #5, mà là giới hạn thật của bản SDK đang pin. Không phát sinh rủi ro bảo mật ngay bây giờ vì Biscuit auth đang tắt hoàn toàn theo thiết kế mặc định (`fiber-node` bind private IP `172.28.0.10`, xem quyết định 2026-07-02 "Cân nhắc 2 hướng xử lý... Human chọn (b)") — chỉ cần lưu ý nếu sau này có ai bật Biscuit auth thật, phải viết thêm 1 custom `Transport` mới forward được token.
  3. **Mapping `asset: "CKB"` → SDK's `Currency` enum**: `newInvoice()` yêu cầu `currency: "Fibb" | "Fibt" | "Fibd"` (không nhận free-string "CKB"/"RUSD" như issue text). 3 giá trị này encode network (mainnet/testnet/devnet-style prefix, giống Lightning's `lnbc`/`lntb`). Vì `fiber-node` của FiberGate chỉ chạy testnet (xem "Kiến trúc tổng thể" + `docker/fiber-node` config), `client.ts` hard-code `asset: "CKB"` → `currency: "Fibt"` — khớp đúng ví dụ dùng trong chính JSDoc của SDK (`sdk.ts`). Cần revisit nếu FiberGate sau này target mainnet.

[2026-07-03] **`paymentPreimage` sinh ở đâu, và scope RUSD của issue #5** (2 quyết định human chốt trực tiếp trong hội thoại trước khi Implementer bắt đầu, xem `harness-brief.md` "Resolved decisions" của run `.claude/harness/20260703T170120/`):
  1. **Preimage (HTLC secret)**: `@ckb-ccc/fiber`'s `newInvoice()` bắt caller tự cung cấp `paymentPreimage` (không tự sinh hộ như 1 số implementation Lightning khác — đây là bí mật dùng để `payment_hash = H(preimage)`, node tự lưu/settle khi HTLC khớp tới, xem giải thích chi tiết đã trao đổi trong hội thoại). Human chọn: `client.ts`'s `createInvoice()` tự sinh preimage nội bộ bằng `crypto.randomBytes(32)` (Node built-in, không phải dependency mới), trả `payment_preimage` ra ngoài cùng `{ invoice_address, payment_hash }`. **Không thêm cột DB nào trong issue #5** — `lib/fiber/` không đụng Postgres theo đúng kiến trúc (`lib/db/` vs `lib/fiber/` tách biệt) — việc có cần lưu preimage vào DB để audit/hỗ trợ khách hàng hay không là quyết định của issue tương lai (route `POST /invoices`, chưa tồn tại), không phải issue #5 — Lý do: giữ đúng scope "lib-only" của issue #5, tránh đụng vào DB schema (CLAUDE.md yêu cầu hỏi riêng nếu cần đổi schema) khi chưa rõ nhu cầu audit thực tế.
  2. **RUSD/UDT scope**: `@ckb-ccc/fiber`'s `Currency` enum chỉ có denomination native CKB (`Fibb`/`Fibt`/`Fibd`), không có giá trị cho RUSD (1 UDT) — SDK có structure UDT riêng (`UdtArgInfo`, `udtScript`) trông như 1 code path khác hẳn, cần investigate riêng. Human chọn: issue #5 chỉ implement CKB thuần; `asset: "RUSD"` throw `UnsupportedAssetError` rõ ràng thay vì âm thầm bỏ qua hay implement dở dang. RUSD/UDT đầy đủ được tách ra **issue #27** (milestone Phase 1 — Core, label `fiber-client`, phụ thuộc #5) — Lý do: tránh Implementer đoán mò 1 code path chưa document rõ trong ngân sách của 1 issue; `.context/business-rules/payment-rules.md`'s BR-INV-002 đã được ghi chú cập nhật tương ứng (đọc "Cập nhật 2026-07-03" ngay dưới rule đó).

[2026-07-04] **Rate-limit counter storage cho BR-RTE-001 (issue #6, REST API `/api/v1/*`)**: BR-RTE-001 yêu cầu chặn 100 invoice/phút trên toàn bộ deployment, nhưng không có cơ chế lưu counter nào được chỉ định trong `.context/` — `system-design.md`'s stack không có Redis/cache layer, chỉ Postgres + 1 container `fibergate-core` chạy dài hạn. Human chọn **in-memory sliding-window counter** trong `apps/web/lib/api/rate-limit.ts` (thay vì DB-backed counter qua `COUNT(*)` hoặc bảng riêng) — Lý do: khớp đúng kiến trúc "1 container dài hạn, không Redis" đã chốt ở `system-design.md`, không tốn thêm query nào trên hot path, không thêm dependency mới; counter reset lúc container restart/redeploy được chấp nhận vì BR-RTE-001 là anti-abuse guard cho prototype, không phải hard financial control. Đánh đổi đã biết: cách này giả định deployment luôn chỉ có 1 replica `fibergate-core` — đúng với kiến trúc hiện tại nhưng sẽ cần đổi sang DB-backed hoặc cache layer riêng nếu sau này scale ra nhiều replica.

[2026-07-04] **`apps/web/package.json`'s `build` script thiếu env loading (phát hiện lúc Checker chạy `pnpm build` cho issue #6)**: Route mới `POST /invoices`/`GET /invoices/:id` import `@/lib/db` ở module scope, khiến Next.js's build-time page-data collection chạy `lib/db/index.ts`'s `requireEnv("POSTGRES_USER"...)` và fail vì `build` script trước đó chỉ chạy `next build` trần, không load `.env` như `dev`/`db:generate`/`db:migrate` (3 script này đều dùng `dotenv -e .env.local -e ../../.env --`, xem quyết định 2026-07-02 "Thêm `dotenv-cli`..."). Human chọn sửa `build` script để khớp đúng pattern 3 script kia (`"build": "dotenv -e .env.local -e ../../.env -- next build"`) thay vì đổi `lib/db/index.ts` sang lazy-init client — Lý do: đây là bug thiếu nhất quán trong chính `package.json`, không phải vấn đề thiết kế của `lib/db/`; sửa theo pattern đã có sẵn tối giản hơn và không đụng vào file thuộc issue #4. Đã verify: `pnpm --filter web build` chạy sạch, 0 lỗi, sau khi sửa.

[2026-07-04] **Shannon→CKB formatting (`GET /invoices`, `GET /invoices/:id`, `GET /node/info`): vendor thuật toán của `@ckb-ccc/core`'s `fixedPointToString`, không thêm `@ckb-ccc/core` làm dependency**: 3 route trên đều cần convert `bigint` shannon sang `number` CKB cho response JSON — bản tự viết ban đầu (`Number(shannon) / 1e8`) có rủi ro mất precision với giá trị lớn (đặc biệt `GET /node/info`'s channel capacity, không bị chặn cận trên như `amount` theo BR-INV-001). Đã cân nhắc thêm `@ckb-ccc/core` (nơi có sẵn `fixedPointToString`/`fixedPointFrom`, decimals=8 khớp đúng shannon) làm dependency tường minh, verify qua `npm view` thấy: `@ckb-ccc/core` có dòng ổn định thật (`latest` = `1.16.1`), nhưng `@ckb-ccc/fiber` (SDK Fiber Network dùng thật trong `lib/fiber/client.ts`) **chưa từng có bản ổn định nào** — npm chỉ có 2 version, cả 2 đều canary — và tự pin `@ckb-ccc/core` nội bộ đúng bằng `0.0.0-canary-20260505020844` (exact, không phải range). Nếu app import `@ckb-ccc/core@1.16.1` trực tiếp (bản mới nhất) sẽ cài song song 2 version `@ckb-ccc/core` khác nhau trong cùng dependency tree (1 canary nội bộ `@ckb-ccc/fiber` dùng, 1 stable app tự import) — không đảm bảo hành vi `fixedPoint` giống hệt giữa 2 bản cách nhau ~1 năm phát triển; muốn dùng đúng version khớp thì phải pin lại đúng canary y hệt `@ckb-ccc/fiber`, chỉ để lấy 1 hàm ~15 dòng. Human chọn: vendor thuật toán (chỉ phần nhận input `bigint`, không port `fixedPointFrom`'s string/number path vì không dùng tới) vào `apps/web/lib/api/format.ts`, comment trích rõ package/version/file nguồn + MIT license (verify qua `npm view @ckb-ccc/core license`) — khớp nguyên tắc "pin đúng ref/provenance" đã áp dụng cho `docker/fiber-node/config.yml` (quyết định 2026-07-02). Không phát sinh dependency mới nào; đã thêm `apps/web/lib/api/format.test.ts` verify port đúng (0, 1 CKB tròn, phân số, đơn vị nhỏ nhất 1 shannon, giá trị lớn hơn cận invoice).

[2026-07-04] **Dashboard auth (issue #9): bcrypt lib, session signing, secret name, expiry, submission mechanism, password storage — 6 quyết định human chốt trực tiếp trước khi Implementer bắt đầu** (xem `harness-brief.md`'s "Needs human decision" của run `.claude/harness/20260704T172749/`):
  1. **bcrypt library**: `bcryptjs` (pure JS) thay vì `bcrypt` (native bindings) — Lý do: tránh rủi ro node-gyp/build-toolchain trên `node:20-alpine` (base image `fibergate-core`, xem quyết định 2026-07-02 "Base images cho docker-compose"), đánh đổi hiệu năng chấp nhận được cho 1 thao tác verify-per-login, không phải hot path.
  2. **Session cookie signing**: `jose` (JWT/JWE, dependency mới) thay vì `iron-session` hoặc tự viết HMAC-SHA256 qua `node:crypto` — Lý do: thư viện được audit, là lựa chọn khuyến nghị chính thức cho Next.js, tránh rủi ro tự implement sai 1 cơ chế crypto quan trọng.
  3. **Env var mới cho session secret**: `DASHBOARD_SESSION_SECRET` — Lý do: tên rõ ràng gắn với dashboard session, không nhầm với `FIBERGATE_INTERNAL_SECRET`/`ADMIN_PASSWORD_HASH`, khớp tinh thần đổi tên đã làm cho `FIBER_NODE_RPC_AUTH_TOKEN` (2026-07-02).
  4. **Session expiry**: sliding/rolling, ~7 ngày (refresh khi có hoạt động) — Lý do: cân bằng giữa UX (admin không bị đăng xuất giữa chừng) và an toàn (session rảnh rỗi vẫn hết hạn).
  5. **Login/logout submission**: Next.js Server Action (không phải Route Handler) — Lý do: form-only flow, không cần expose endpoint JSON public, tự động có CSRF protection qua Origin-header check built-in của Server Actions.
  6. **Password storage**: giữ nguyên env-var-only cho issue #9 (không chuyển sang DB), nhưng tách việc chuyển sang DB-backed + flow đổi mật khẩu trong app thành issue riêng — đã tạo **issue #30** (không gắn milestone, vì đây là cải tiến sau hackathon, không thuộc Phase 0-4) — Lý do: giữ đúng scope issue #9 theo đúng nội dung issue gốc + US-001 (chỉ yêu cầu rotate qua env var + restart container), không tự ý mở rộng thêm 1 DB schema change chưa được yêu cầu. Đóng luôn Open Question 2026-07-02 "`ADMIN_PASSWORD_HASH` nên ở env var hay chuyển vào DB?" — xem issue #30 cho phần chưa quyết (migration/seed behavior, UI đổi password).

[2026-07-04] **Test tooling cho `/api/v1/*`: Vitest (unit) + `@usebruno/cli` (HTTP integration), cả 2 đều là devDependency mới của `apps/web` (human xác nhận trực tiếp trong hội thoại trước khi gọi harness, đúng quy trình CLAUDE.md yêu cầu cho "cài thêm dependency mới")**: `apps/web/package.json` thêm `vitest` (unit test cho `lib/api/*` và route handler trong `app/api/v1/**`, mock ở boundary `@/lib/db` + `@/lib/fiber/client` theo đúng CLAUDE.md's "Fiber RPC calls" — không mock `@ckb-ccc/fiber` trực tiếp) và `@usebruno/cli` (chạy collection HTTP thật qua `bru run`, verify cú pháp thật bằng `bru run --help` sau khi cài thay vì đoán mò). `apps/web/vitest.config.ts` resolve alias `@/*` bằng `resolve.alias` thuần của Vitest (không thêm `vite-tsconfig-paths` — dùng được với 1 alias duy nhất, không cần thêm dependency). Bruno collection đặt ở root `bruno/` (cross-cutting artifact, ngang hàng `docker-compose.yml`, không thuộc riêng `apps/web`) — `apps/web/package.json`'s `test:integration` script `cd ../../bruno && bru run --env local` (verify: `bru run`'s `collectionPath` luôn là `process.cwd()`, không nhận path argument trỏ tới collection ở nơi khác — đọc source `@usebruno/cli`'s `src/commands/run.js` xác nhận). Secret handling cho Bruno theo đúng pattern `.env.example` đã có: `bruno/environments/local.bru` chỉ chứa reference `{{process.env.FIBERGATE_INTERNAL_SECRET}}`, KHÔNG dùng Bruno GUI-native "secret" var marking (`vars:secret [...]` block) vì cơ chế đó không đọc được từ `bru run` CLI (chỉ dùng được trong GUI) — giá trị thật chỉ nằm ở `bruno/.env` (gitignored, mirror `bruno/.env.example` đã commit), đọc qua `process.env.*` interpolation Bruno hỗ trợ sẵn cho `.env` ở root collection. Đã verify sống toàn bộ luồng: dựng `postgres` + `fiber-node` qua `docker compose up -d`, chạy `pnpm --filter web dev`, `pnpm --filter web test:integration` chạy 10/10 request, 21/21 test pass qua HTTP thật; `pnpm --filter web test:unit` 69/69 test pass kể cả khi unset toàn bộ `POSTGRES_*`/`FIBER_NODE_URL` (chứng minh mock chặn đúng module boundary, không rơi xuống implementation thật) — Lý do: khớp đúng 2 lựa chọn human đã chốt trước khi harness này chạy (Vitest thay vì Jest, cần cả unit lẫn integration test), không phát sinh quyết định implementation-level mới nào ngoài dự kiến.

[2026-07-04] **`CRON_SECRET` unset → `POST /api/cron/poll-invoices` trả `503`, không throw qua `requireEnv()` (issue #7)**: `system-design.md` ghi `CRON_SECRET` là optional, nhưng copy nguyên `requireAuth()`'s pattern (dùng `requireEnv()`) sẽ biến "optional" thành crash 500 nếu merchant không set. Human chốt (xem `harness-brief.md` "Resolved decisions" của run `.claude/harness/20260704T171457/`): thêm accessor không throw `getOptionalEnv()` trong `apps/web/lib/env.ts`, chỉ dùng riêng cho route này — `requireEnv()` giữ nguyên hành vi throw cho `FIBERGATE_INTERNAL_SECRET` và các biến required khác. Route trả `503 CRON_NOT_CONFIGURED` nếu `CRON_SECRET` rỗng/unset (endpoint coi như bị tắt), chỉ chạy check Bearer token (constant-time, tách riêng qua `requireBearerToken()` trong `apps/web/lib/api/auth.ts` để không copy-paste logic so sánh) khi secret đã được cấu hình — Lý do: khớp đúng tinh thần "optional" đã document, tránh trải nghiệm xấu (500 không rõ ràng) cho merchant không cần dùng manual-trigger endpoint này. Đã thêm `CRON_SECRET` vào `.env.example` kèm comment optional.

[2026-07-04] **Webhook trigger mechanism cho issue #7, trong khi issue #8 (Webhook delivery system: HMAC sign, retry, delivery log) còn open**: Human chốt (xem `harness-brief.md` "Resolved decisions" của run `.claude/harness/20260704T171457/`) phương án **stub trigger function** thay vì (a) implement inline single-attempt HMAC-signed POST ngay bây giờ, hoặc (c) chỉ để code-comment không có function gọi được. Thêm `apps/web/lib/webhooks/trigger.ts` export `triggerWebhook(invoice, eventType)` — poller (`apps/web/lib/poller/invoice-poller.ts`) gọi hàm này ở mọi transition sang terminal state (`paid`/`expired`/`failed`), nhưng body hiện tại chỉ `console.log` kèm `// TODO(#8): implement HMAC-signed delivery + retry + webhook_deliveries persistence`, không có HTTP call thật. Không đụng `webhook_endpoints`/`webhook_deliveries` tables (schema giữ nguyên) — Lý do: implement HMAC-sign + retry đầy đủ ngay trong issue #7 sẽ trùng lặp scope của #8 (đã có sẵn, unassigned); để trống hoàn toàn (phương án c) thì acceptance criterion "on terminal transition, trigger webhook" của issue #7 không có gì verify được. Stub function vừa giữ đúng ranh giới 2 issue, vừa để sẵn call site cho #8 sau này chỉ cần đổi phần thân hàm.

[2026-07-05] **Service/use-case layer cho `/api/v1/*` + `/node/info`, không dùng full MVC folder convention**: Human nêu cảm nhận database/API implementation hiện tại "rời rạc" và hỏi có nên đổi sang MVC cho Next.js không. Đã thảo luận: Next.js App Router (API-only, không server-render view) không map tốt vào MVC cổ điển (không có "View" thật). Human chốt hướng **service/use-case layer**: thêm `apps/web/lib/services/invoices.ts` (`createInvoice`/`listInvoices`/`getInvoiceById`, gồm cả `InvoiceCursor`/`encodeCursor`/`decodeCursor` dời từ route sang) và `apps/web/lib/services/node.ts` (`getNodeStatus`) — `route.ts` trong `app/api/v1/invoices/`, `app/api/v1/invoices/[id]/`, `app/api/v1/node/info/` giờ chỉ còn auth, parse/validate request, map lỗi sang HTTP status, và response shaping; mọi Drizzle query + Fiber RPC call dời hết vào service. Rate limit (`BR-RTE-001`, `tryConsumeInvoiceCreationSlot()`) **giữ nguyên ở route**, không dời xuống service — theo đúng taxonomy `BR-RTE-*` (route-level) vs `BR-INV-*` (invoice domain) đã có sẵn trong `payment-rules.md`. `lib/services/invoices.ts`'s `createInvoice()` giữ nguyên 2 try/catch riêng biệt (Fiber call vs. DB insert) thay vì gộp làm 1 — gộp lại sẽ mất khả năng phân biệt "Fiber đã tạo invoice nhưng ghi DB thất bại" (rủi ro orphaned payment_hash) với "chính call Fiber thất bại" trong log, dù response HTTP ra ngoài giống hệt nhau ở cả 2 trường hợp. `lib/services/node.ts` trả thẳng object dạng snake_case khớp API response (không tạo thêm domain type + serializer riêng) vì endpoint này không có DB model tách biệt khỏi wire shape và chỉ có 1 consumer — cố tình bất đối xứng so với `invoices.ts` (trả `InvoiceRow` thô, serialize ở route) vì `InvoiceRow` còn được `lib/poller/invoice-poller.ts` dùng, cần giữ nguyên shape DB. Test đổi theo 2 tầng: `lib/services/*.test.ts` mock `@/lib/db` + `@/lib/fiber/client`; 3 `route.test.ts` mock `@/lib/services/*` thay vì mock db/fiber trực tiếp — cùng số lượng assertion, chỉ đổi boundary (ví dụ `[id]/route.test.ts`'s "404 không query DB cho UUID sai" dời xuống `invoices.test.ts`). **Ngoài phạm vi**: `lib/poller/invoice-poller.ts`'s DB access trực tiếp giữ nguyên — background job khác bản chất với request/response controller, và vừa được implement + review ở issue #7, có thể gộp vào service layer sau như 1 bước riêng nếu cần — Lý do: tách rõ HTTP/response concern (route) khỏi business logic (service), giải quyết đúng vấn đề "DB access rải rác nhiều route.ts, không có 1 chỗ để tra" mà không gượng ép áp full MVC convention (controllers/models/views) vốn không khớp kiến trúc API-only của Next.js App Router. Đã verify: `pnpm --filter web typecheck` sạch, `pnpm --filter web test:unit` 104/104 pass, `pnpm lint` sạch.
[2026-07-05] **Dashboard auth (issue #9) — code review trước khi tạo PR phát hiện 2 lỗi nghiêm trọng + 2 quyết định thiết kế human chốt trực tiếp:**

Chạy `/code-review` (8 agent song song: 3 correctness angle + reuse/simplification/efficiency/altitude + conventions) trên diff issue #9 trước khi commit/tạo PR. Tự verify lại từng candidate bằng công cụ thật (build, `docker compose config`, đọc trực tiếp Dockerfile/README) thay vì chỉ tin lời agent. 2 lỗi sau nếu không phát hiện sẽ khiến feature hoàn toàn không hoạt động ở target deploy thật:

1. **`middleware.ts`'s `config.matcher` import identifier từ module khác — Next.js không static-extract được, âm thầm fallback "guard mọi route"**: verify bằng `next build` thật thấy warning `"Next.js can't recognize the exported 'config' field... The default config will be used instead"`. Hậu quả nếu không fix: `/login` tự guard chính nó → infinite redirect loop; mọi request `/api/v1/*`/`/api/cron/*` (auth qua Bearer header, không phải session cookie) bị chặn nhầm redirect về `/login`, phá vỡ toàn bộ REST API. Fix: inline matcher array literal trực tiếp trong `middleware.ts`'s `config` export thay vì import từ `lib/auth/routes.ts` — xem chi tiết + hệ quả (matcher giờ là allowlist thủ công, thêm page mới dưới `(dashboard)/` phải tự thêm path pattern) ở `system-design.md`'s "Dashboard auth: session cookie + middleware guard".

2. **`docker-compose.yml`'s `fibergate-core.environment` thiếu `DASHBOARD_SESSION_SECRET`**: Compose chỉ inject đúng những biến liệt kê tường minh trong `environment:` block, không tự forward toàn bộ root `.env` — biến mới thêm ở quyết định 2026-07-04 bị bỏ sót khỏi block này. `pnpm dev` không lộ bug vì script đó load thẳng root `.env`, bỏ qua allowlist của Compose. Verify bằng `docker compose config | grep DASHBOARD_SESSION_SECRET` trước/sau fix. Bài học: mọi biến app mới thêm vào `.env.example` phải đối chiếu lại `docker-compose.yml`'s `fibergate-core.environment` trong cùng session, xem `system-design.md`'s "Environment Variables".

Sau 2 fix trên, human tiếp tục chốt 2 quyết định thiết kế phát sinh từ review (không tự quyết vì thuộc phạm vi auth/session logic của CLAUDE.md):

3. **Cookie `Secure` flag dựa vào `X-Forwarded-Proto` header (request scheme thực tế), không dựa vào `NODE_ENV`**: `Dockerfile` hardcode `NODE_ENV=production` nhưng docker-compose bundle không có TLS termination built-in (`fibergate-core` publish thẳng port `3000:3000`, plain HTTP) — nếu gắn `Secure` theo `NODE_ENV`, browser sẽ âm thầm từ chối lưu cookie trên chính flow deploy mặc định, login trông như thành công nhưng session không lưu, không có error message. `isHttpsRequest()` đọc `X-Forwarded-Proto` — không có header (mặc định, không proxy) thì `secure: false`; nếu sau này thêm nginx/Caddy TLS termination, tự động đúng mà không cần sửa code, miễn nginx set đúng header và `fibergate-core`'s port đổi sang không publish trực tiếp (tránh ai đó bỏ qua nginx tự set header giả). Xem `payment-rules.md`'s BR-SEC-004 + `system-design.md` để biết chi tiết.

4. **Giữ nguyên `cachedSigningKey`** (memoize signing key trong `session.ts`, thêm ở 1 `/simplify` pass trước đó) dù 2/3 review agent (Simplification, Altitude) cho là thừa (thao tác `TextEncoder` chỉ tốn micro giây) — Lý do human chốt: `verifySessionToken` chạy trên gần như mọi request/page load dưới `(dashboard)/**`, tần suất gọi đủ cao để giữ cache hợp lý dù lợi ích tuyệt đối nhỏ.

5. **`verifySessionToken()` không nuốt lỗi thiếu `DASHBOARD_SESSION_SECRET`** — tách biệt khỏi trường hợp token invalid/expired (trước đó catch-all coi 2 case như nhau, đều redirect `/login` im lặng, không log gì). Giờ `getSigningKey()`'s throw (do biến env thiếu) nằm ngoài `try`, propagate ra ngoài (middleware sẽ crash rõ ràng thay vì âm thầm redirect) — khớp pattern `lib/api/auth.ts`'s `requireAuth()`, vốn cũng để lỗi thiếu `FIBERGATE_INTERNAL_SECRET` throw uncaught.

[2026-07-05] **`ADMIN_PASSWORD_HASH` → `ADMIN_PASSWORD_HASH_B64` (issue #9, sau khi PR #32 merge) — phát hiện lúc human tự test login qua `pnpm dev`, không phải qua `docker compose up -d`:**

Human thấy `bcrypt.compare()` fail dù password đúng lúc chạy `pnpm dev` local, dùng hash đã verify đúng bằng `bcrypt.compareSync()` trực tiếp (script test riêng). Điều tra kỹ hơn phát hiện đây **không phải** đơn giản là quên escape `$$` như bug đã ghi ở 2026-07-02 — mà là 1 xung đột kiến trúc thật giữa 2 cơ chế cùng đọc chung root `.env`:

- **Docker Compose**: tự interpolate `$VAR`/`${VAR}` bên trong giá trị `.env`, coi `$$` là escape hợp lệ cho 1 dấu `$` literal (đã verify bằng container thật qua `docker compose run --rm test printenv TESTVAR` — **không chỉ tin `docker compose config`'s output**, vì lệnh đó tự re-escape `$` lúc hiển thị YAML nên trông như đúng dù giá trị runtime thật sai). Kết quả: `$$2y$$10$$abc` trong `.env` → đúng `$2y$10$abc` trong container.
- **`dotenv-expand`** (dependency của `dotenv-cli`, dùng bởi 4 script `dev`/`build`/`db:generate`/`db:migrate`): cũng tự interpolate `$VAR` nhưng theo thuật toán khác, **không** coi `$$` là escape — cả giá trị raw (`$2y$10$abc`) lẫn giá trị đã escape (`$$2y$$10$$abc`) đều bị corrupt (mất hẳn 1 đoạn ký tự giữa chừng), chỉ khác nhau ở chỗ bị mất đoạn nào.

Đã thử 2 hướng workaround trước khi kết luận không có cách escape nào chung: (a) `dotenv-cli`'s flag `--no-expand` — làm giá trị raw pass-through đúng, nhưng giá trị `$$`-escaped (cần cho Docker) thì vẫn sai (không tự collapse); (b) Docker Compose's `env_file:` directive thay cho `environment: ${VAR}` — verify bằng container thật vẫn bị corrupt y hệt, Compose áp dụng interpolation cho **mọi** cách đọc `.env`, không riêng gì cách đang dùng.

Nêu 3 hướng fix cho human trước khi chọn: (a) base64-encode giá trị trong `.env` (bảng chữ base64 không có `$`, né hoàn toàn cả 2 cơ chế); (b) thêm `--no-expand` + chấp nhận `pnpm dev` cần giá trị khác Docker, phá vỡ quyết định "1 file `.env` duy nhất" (2026-07-02); (c) chỉ document hạn chế, không sửa code, chấp nhận không test được login qua `pnpm dev`.

**Human chọn (a)** — Lý do: sửa đúng gốc rễ (loại bỏ hẳn ký tự `$` khỏi giá trị lưu trong `.env`), không cần escape gì nữa ở cả 2 phía, không phá quyết định "1 file `.env` duy nhất". Implement: đổi tên env var thành `ADMIN_PASSWORD_HASH_B64` (tên mới để không ai nhầm lẫn dán raw hash vào — khác hẳn giá trị cũ nên không có rủi ro tương thích ngược với hash cũ nào đang chạy thật), `README.md`'s "Generating secrets" thêm bước `| base64 | tr -d '\n'` vào lệnh generate, `app/login/actions.ts` decode bằng `Buffer.from(value, "base64").toString("utf-8")` trước khi `bcrypt.compare()`. Biết trước đây là fix tạm cho model env-var-only — nếu issue #30 (chuyển `ADMIN_PASSWORD_HASH` sang DB-backed) triển khai, vấn đề này biến mất hoàn toàn cho việc verify hàng ngày (Postgres/Drizzle không quan tâm `$`), chỉ còn đúng 1 lần lúc seed dữ liệu ban đầu cần thiết kế riêng.

[2026-07-06] **`@fibergate/sdk`'s `webhooks.verify()` HMAC scheme (issue #11, xem `harness-brief.md`'s "Resolved decisions" #1 của run `.claude/harness/20260706T141049/`)**: Human xác nhận trực tiếp trong hội thoại (trước khi Implementer bắt đầu, đúng quy trình CLAUDE.md yêu cầu cho auth/signing logic): SDK's `verify(body, signature, secret)` mirror y hệt scheme đã implement server-side ở `apps/web/lib/webhooks/sign.ts` — `HMAC-SHA256(secret, rawBodyUtf8Bytes)` → hex → `` `sha256=${hex}` ``, `signature` param nhận nguyên header value (kèm prefix `sha256=`), so sánh bằng `crypto.timingSafeEqual` (constant-time, có length-check trước để tránh throw trên input dị dạng/ngắn) — không dùng `===` như sample minh hoạ trong `rest-api-spec.md`'s "Webhook Payload" section (sample đó chỉ để minh hoạ ý tưởng, không phải pattern bảo mật đã duyệt) — Lý do: verify phải khớp đúng cách server đã sign, không có phương án thay thế hợp lý nào khác; vẫn hỏi qua human theo đúng blanket rule của CLAUDE.md cho mọi logic signing/hashing dù câu trả lời gần như hiển nhiên.

[2026-07-06] **`@types/node` — thêm devDependency mới cho `packages/sdk` (issue #11)**: Implementer phát hiện lúc code `webhooks.ts` (dùng `node:crypto`/`Buffer`) rằng `packages/sdk` chưa có type declaration cho Node built-ins. Human xác nhận thêm `@types/node: ^20.19.43` vào `packages/sdk/package.json`'s `devDependencies` (khớp version đã pin ở `apps/web`) — dev-only, không ship trong `dist/`, không phải runtime dependency — Lý do: cần thiết để type-check `node:crypto` cho `verify()` (đã chốt dùng API đồng bộ ở quyết định ngay trên; Web Crypto API bất đồng bộ sẽ vi phạm signature `boolean`-returning đã chốt).

[2026-07-06] **Thêm `vitest` làm devDependency của `packages/sdk` cho unit test (issue #11, sau khi PR ban đầu đã merge/review)**: Human chủ động hỏi có cần bổ sung test case cho SDK không — đồng ý cần, vì SDK này được merchant bên thứ ba cài làm dependency, lỗi âm thầm ở đây tốn kém hơn code nội bộ. Human xác nhận trực tiếp cách cài đặt (đúng quy trình CLAUDE.md cho "cài thêm dependency mới"): pin `vitest: ^4.1.9` — khớp đúng version `apps/web` đang dùng (tự dedupe qua pnpm content-addressable store, không cài trùng lặp thật trên disk) — khai báo trực tiếp trong `packages/sdk/package.json`'s `devDependencies`, **không** hoist lên root `package.json` (root hiện không có `devDependencies` nào, giữ đúng convention `tsup`/`typescript` đã khai riêng theo từng package, tránh phantom-dependency). Thêm `packages/sdk/vitest.config.ts` tối giản hơn bản `apps/web` (không cần `resolve.alias` vì SDK không có path alias `@/*`, chỉ dùng relative import). Thêm 2 script `test:unit`/`test` (alias thẳng, không có integration test riêng cho SDK) — đặt tên `test` khớp `apps/web` để root's `pnpm test` (`pnpm -r --if-present test`) tự động chạy luôn không cần sửa gì ở root. Test coverage ưu tiên theo rủi ro: `webhooks.test.ts` (ưu tiên cao nhất — security-critical, cover valid signature/wrong secret/tampered body/thiếu prefix `sha256=`/signature ngắn hoặc dị dạng/rỗng, tất cả phải trả `false` chứ không throw) và `client.test.ts` (mock global `fetch`, cover request shaping + `FiberGateApiError` mapping cho `invoices.*`/`node.getInfo()`) — Lý do: SDK là public API surface, ưu tiên đúng phần rủi ro cao nhất (signing/verification logic) trước, không viết test tràn lan cho mọi hàm. Đã verify: `pnpm --filter sdk test:unit` 18/18 pass, `pnpm -r --if-present test` xác nhận sdk's test tự chạy qua root script không cần thêm wiring, `pnpm --filter sdk typecheck`/`build`/`pnpm lint` đều sạch.

[2026-07-07] **Webhook signing secret "reveal" UX cho Dashboard pages (issue #10, xem `harness-brief.md`'s "Resolved decisions" #1 của run `.claude/harness/2026-07-06T184210/`)**: Mockup Claude Design (`FiberGate.dc.html`) show 1 nút reveal/copy signing secret luôn sẵn sàng trên trang Webhooks, nhưng service layer đã chốt từ issue #8 chỉ trả plaintext secret **đúng 1 lần** lúc tạo endpoint (`createWebhookEndpoint()`), sau đó không thể lấy lại (`decryptWebhookSecret()` chỉ dùng nội bộ cho HMAC sign, không phục vụ hiển thị lại). Human chọn **giữ nguyên security posture đã chốt** thay vì implement mockup y nguyên: trang Webhooks hiện masked placeholder (`whsec_••••••••`) + nút "Regenerate secret" (service function mới, phát hành secret mới và hiện đúng 1 lần y hệt lúc tạo) — không có nút "reveal" nào truy xuất lại secret cũ — Lý do: đi theo mockup literal sẽ walk back quyết định bảo mật đã chốt ở issue #8 (BR-SEC-003), và đúng loại quyết định CLAUDE.md yêu cầu hỏi human (auth/signing).

[2026-07-07] **Quick Start step 3 ("Install the SDK — Mark as done") persistence cho Dashboard pages (issue #10, xem `harness-brief.md`'s "Resolved decisions" #2 của run cùng trên)**: Không có cột DB nào backing riêng cho step này (chỉ steps 1/2/4/5 derive được từ dữ liệu thật sẵn có — deploy/login luôn done, có invoice paid, có webhook endpoint). Human chọn **client-side only qua `localStorage`** (`use-quick-start-progress.ts`), không thêm cột/bảng DB mới — Lý do: đơn giản, không đổi schema; đánh đổi đã biết: tick của step 3 không đồng bộ giữa nhiều trình duyệt/thiết bị (chấp nhận được vì single-admin, thường chỉ dùng 1 máy).

[2026-07-07] **Route slug cho trang Delivery Log (issue #10, đổi tên từ "Settlement Log" — tránh trùng khái niệm "settlement" đã dùng cho channel-level TLC fulfillment, xem BR-STS-001)**: Giữ `/delivery-log` theo convention "slug khớp nav label" đã áp dụng cho các trang khác (`/invoices`, `/webhooks`, `/quick-start`) — human xác nhận trực tiếp khi được hỏi, không đổi sang phương án khác (`/deliveries`) được đề xuất.

---

## Business Logic

[2026-07-04] **Node status → `invoices.status='paid'` mapping (issue #7, background poller)**: `@ckb-ccc/fiber`'s `CkbInvoiceStatus` có 5 giá trị (`"Open" | "Cancelled" | "Expired" | "Received" | "Paid"`), nhưng `.context/business-rules/payment-rules.md`'s BR-STS-001/002/003 trước đó chỉ định nghĩa trigger cho `expired`/`failed`, chưa bao giờ chốt giá trị nào map sang `paid`. Human chốt (xem `harness-brief.md` "Resolved decisions" của run `.claude/harness/20260704T171457/`): **chỉ `"Paid"` map sang `invoices.status = 'paid'`**, xác nhận qua Fiber's Rust source thật (`crates/fiber-types/src/invoice.rs`'s doc comment "the invoice is received, but not settled yet" cho `Received`; `crates/fiber-lib/src/fiber/channel.rs:1898` chỉ set `CkbInvoiceStatus::Paid` khi TLC được remove với `RemoveTlcFulfill`, tức hoàn tất handshake settlement ở tầng channel). `"Received"` không phải trạng thái terminal — invoice giữ nguyên `pending`, không đổi status ở cycle đó. Vì `lib/fiber/client.ts`'s `createInvoice()` đã cung cấp preimage cho node ngay lúc tạo invoice (không phải hold-invoice flow), `Received → Paid` diễn ra tự động gần như tức thời ở phía node — poller sẽ quan sát được `"Paid"` ở cycle 10s kế tiếp mà không cần thêm hành động nào để ép settlement — Lý do: tránh đánh dấu `paid` trước khi settlement thật sự hoàn tất (rủi ro tài chính nếu coi `Received` là đủ), đồng thời tránh never-paid nếu bỏ sót `Paid` khỏi mapping. Implement tại `apps/web/lib/poller/invoice-poller.ts`'s `applyNodeStatus()`.

[2026-07-06] **Webhook retry processing (issue #8, `BR-WHK-003`): tách biệt khỏi cơ chế detect invoice status, không dùng queue engine ở scope hiện tại** (human xác nhận trực tiếp trong hội thoại): Xử lý attempt 2/3 (retry đến hạn `next_retry_at`) về bản chất là **time-based**, không phải event-based — cần biết "bây giờ đã đủ 1 phút/5 phút chưa", không có sự kiện blockchain nào tự báo điều đó. Việc này khác hẳn "phát hiện invoice chuyển terminal state → bắn attempt 1 ngay" (event-driven: poller diff ở Phase 1, WebSocket `subscribe_store_changes` handler ở Phase 2) — 2 concern này nên tách biệt trong code dù ở Phase 1 chúng tình cờ dùng chung 1 interval timer. Kết luận: **giữ 1 interval scanner riêng, độc lập** (không gộp chung với cơ chế detect invoice status) quét `webhook_deliveries WHERE status='pending' AND next_retry_at <= now()` — không bị ảnh hưởng nếu sau này Phase 2 đổi cách detect invoice status sang WebSocket (theo `system-design.md` dòng 81-101, Phase 2 vẫn giữ poll fallback 30-60s, không bỏ hẳn poll). **Không dùng queue engine riêng** (Redis+BullMQ, hay Postgres-native như `pg-boss`/`graphile-worker`) cho retry này — Lý do: single-tenant, self-hosted, `fibergate-core` chạy 1 process duy nhất không horizontal-scale, nên giá trị cốt lõi của 1 job queue (điều phối nhiều worker/instance, chống trùng lặp xử lý, backpressure ở throughput lớn) không phát sinh ở scope này; bảng `webhook_deliveries` đã tự đủ state cần cho retry scheduling (`attempt_count`, `next_retry_at`, `status`) mà không cần thêm engine/dependency nào. Nếu tương lai có nhiều instance `fibergate-core` chạy song song (ngoài scope hiện tại), nên dùng atomic claim pattern thuần SQL (`UPDATE ... WHERE status='pending' ... RETURNING`, tương đương `SELECT ... FOR UPDATE SKIP LOCKED`) để tránh 2 instance cùng xử lý 1 delivery, thay vì đổi sang thêm dependency queue mới.

[2026-07-06] **Cập nhật/supersede quyết định retry-processing ngay phía trên (issue #8, xem `harness-brief.md` "Resolved decisions" #2 của run `.claude/harness/20260705T224919/`)**: Sau khi chốt hướng "interval scanner riêng" ở mục ngay trên, human tiếp tục hội thoại và **đổi sang cơ chế khác, thay thế hoàn toàn** lựa chọn interval scanner: `setTimeout`-based scheduler (`apps/web/lib/webhooks/retry-scheduler.ts`'s `scheduleAttempt(deliveryId, delayMs)`) + 1 lần boot-recovery scan duy nhất (`recoverPendingDeliveries()`, gọi từ `instrumentation.ts` lúc process khởi động, quét đúng 1 lần `WHERE status='pending' AND next_retry_at IS NOT NULL`) — không còn periodic interval scanner nào chạy lặp lại. Lý do đổi: 1 interval scanner định kỳ (kể cả tần suất thấp) tốn query DB phần lớn thời gian quét vào lúc bảng rỗng, và luôn có độ trễ (drift) giữa `next_retry_at` thật và lúc scanner tình cờ quét tới, trong khi `setTimeout` bắn đúng thời điểm cần. Lập luận "tách biệt time-based retry khỏi event-based status detection" ở mục trên **vẫn đúng và không đổi** — chỉ cơ chế thực thi phần time-based đó đổi từ "quét theo chu kỳ" sang "hẹn giờ chính xác theo từng delivery". Registry `Map<deliveryId, NodeJS.Timeout>` in-memory thay cho việc dựa vào interval query để biết "đến lượt chưa" — đánh đổi đã biết: không durable qua restart/crash, nên `recoverPendingDeliveries()` là cơ chế phục hồi duy nhất, dựa vào `next_retry_at` (nguồn sự thật) vẫn được lưu trong DB như thiết kế ban đầu. Kết luận "không dùng queue engine riêng" (Redis/BullMQ/pg-boss/graphile-worker) ở mục trên vẫn giữ nguyên, không đổi.

[2026-07-05] **Bug: thứ tự bulk clock-expire vs. RPC poll trong `runPollCycle()` (phát hiện qua `/code-review` toàn diện trên nhánh `feat/#7-background-poller-worker`, không phải human decision mà là bug fix theo đúng invariant BR-STS-001 đã có sẵn)**: `runPollCycle()` ban đầu chạy bulk clock-expire (BR-STS-002(b)) TRƯỚC RPC poll batch (BR-POL-002/003). Vì bước clock-expire chỉ dựa vào `expires_at < now()`, không hỏi Fiber node, nó có thể đánh dấu `expired` một invoice vừa được trả tiền đúng lúc/ngay sau khi hết hạn — trước khi RPC poll kịp thấy trạng thái `"Paid"` thật từ node. Do BR-STS-001 chỉ đi 1 chiều (không reverse), invoice này kẹt vĩnh viễn ở `expired`, merchant không bao giờ nhận `payment.paid` dù khách đã trả tiền thật — bug tài chính nghiêm trọng nhất phát hiện được trong review. Fix: đổi thứ tự thành RPC poll batch chạy TRƯỚC, bulk clock-expire chạy SAU trong cùng `apps/web/lib/poller/invoice-poller.ts`'s `runPollCycle()` — vì RPC poll batch (BR-POL-002) đã tự loại trừ invoice hết hạn quá 60s, bulk clock-expire chạy sau chỉ còn bắt đúng nhóm "hết hạn lâu, chưa từng được RPC check", không tranh chấp với nhóm vừa được RPC poll xác nhận `paid` (invoice đó đã rời khỏi `status='pending'` nên WHERE clause của bulk-expire tự động bỏ qua). Đã thêm 2 test case mới trong `invoice-poller.test.ts` verify cả outcome (paid, không phải expired) lẫn thứ tự gọi mock (`invocationCallOrder`). Cùng đợt review cũng fix: phạm vi try/catch trong `pollOneInvoice()` quá hẹp (chỉ bọc `getInvoiceStatus()`, không bọc `applyNodeStatus()` — 1 lỗi DB/webhook giữa batch làm sập cả phần còn lại của batch); thêm re-entrancy guard cho `setInterval` trong `worker.ts` (tránh nhiều cycle chồng lên nhau khi Fiber node chậm). Xem `architecture/system-design.md`'s "Data Flow — Tạo Invoice" mục 3 và `business-rules/payment-rules.md`'s BR-STS-002 để biết thứ tự đúng.

---

## Scope

[2026-07-01] **Phased rollout của tính năng nâng cao**: Websocket/JSON-RPC real-time invoice listener (Phase 2) và L402 subscription middleware (Phase 3, optional stretch) đều nằm trong scope nộp bài, nhưng triển khai theo phase riêng biệt sau khi Phase 1 (docker-compose core: invoice + webhook + dashboard cơ bản) chạy ổn định — Lý do: Giảm rủi ro thời gian trong 15 ngày hackathon; ưu tiên có 1 core flow chạy chắc trước khi thêm tính năng nâng cao.

[2026-07-01] **Đối chiếu với thể lệ hackathon chính thức** (campaign page, user paste nguyên văn): Category đã chọn "Merchant, Liquidity, LSP, and Multi-Asset Infrastructure" — khớp với ví dụ "Merchant checkout SDKs, payment processor prototypes, hosted payment pages, or payment status webhooks" trong đề bài. Phát sinh 3 quyết định bổ sung:
  1. **Naming**: Đổi mô tả từ "LSP framework" sang "merchant payment gateway framework" trong `INDEX.md`/`CLAUDE.md` — Lý do: FiberGate không cung cấp dịch vụ liquidity/mở channel hộ bên thứ ba (đúng nghĩa LSP), tránh overclaim khiến giám khảo hiểu sai scope thực tế.
  2. **Demo bắt buộc**: Thêm "Demo merchant checkout flow" + "1 instance demo public/hosted" vào IN SCOPE Phase 1 (`project-vision.md`) — Lý do: Deliverables hackathon yêu cầu bắt buộc "demo link... plus a hosted demo"; kiến trúc self-hosted-only (không có URL cố định) không tự động thoả điều kiện này nếu không tự deploy 1 bản demo.
  3. **License**: Chọn **MIT** cho toàn bộ repo (mặc định phổ biến cho hackathon open-source, đơn giản, permissive) — Lý do: Deliverables yêu cầu "repository link with fully open-sourced code", cần 1 license rõ ràng. Có thể đổi sau nếu cần, chưa tạo file `LICENSE` trong phiên này.

---

## Open Questions

> Những vấn đề chưa được chốt. Xóa dòng khi đã có quyết định
> và chuyển lên section tương ứng ở trên.

<!-- Thêm vào đây khi phát sinh câu hỏi cần chốt -->